name: Keep Supabase alive
on:
  schedule:
    - cron: '0 0 */3 * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl -sf "${{ secrets.SUPABASE_URL }}/rest/v1/" -H "apikey: ${{ secrets.SUPABASE_PUBLISHABLE_DEFAULT_KEY }}" > /dev/null
